# Git history
.git
.gitignore

# Build / install artifacts (re-built inside container)
build/
install/
log/

# Large zip archives
*.zip

# Python cache
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
**/.pytest_cache/

# Editor / OS junk
.vscode/
.idea/
*.swp
*~
.DS_Store

# Docker files (not needed inside the image itself)
docker/
Dockerfile
docker-compose.yml
.dockerignore

# README extras
README2.md
